# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
venv/
venv_old/

# OEM customizations (local only, not tracked)
app/static/oem/

# Logs
logs/

# Sensitive config / secrets
config/secrets.key
config/ssl/
.secrets.baseline

# The transcript database. It holds what was actually said in a service —
# a congregation's own words — and this repository is public. It was only
# ever untracked, which is one `git add -A` away from being published.
data/
*.db

# Tooling reports
pip_audit_report.json
bandit_report.json

# IDEs
.idea/

# macOS
.DS_Store
/app/static/oem/images/
